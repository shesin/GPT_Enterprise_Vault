import { SmartBeadsEngine } from '../SmartBeadsEngine'; import { FourFourConfig } from '../../config/prototypes/4x4Config'; describe('SmartBeadsEngine', () => { it('initializes with 4x4 config', () => { const engine = new SmartBeadsEngine(FourFourConfig); expect(engine.currentState.board).toEqual(FourFourConfig); expect(engine.currentState.currentPlayer).toBe('RED'); expect(engine.currentState.moveCount).toBe(0); }); });