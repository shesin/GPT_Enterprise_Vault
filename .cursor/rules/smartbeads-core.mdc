---
description: SmartBeads hooks — pointers only; detail in RULES + DECISIONS
alwaysApply: true
---

# SmartBeads (always-on)

When work touches `PROJECTS/SmartBeads/`:

- **STOP** → `instruction-fidelity.mdc` § Before any repo edit
- **Gameplay / UI / colours / modes** → read `GPT_PROJECT_DECISIONS_05P.md` when needed
- **Engineering / agent workflow** → read `GPT_PROJECT_RULES_01P.md` when needed; gates → `VISION/CURSOR_PROMPT_01.md`
- **Shipped vs claim** → `GPT_PROJECT_STATUS_01P.md` § Integrity
- **PENDING** → human-owned, read-only → RULES § PENDING Human-Owned
- **Doc edits** → DECISIONS / AUDIT / VISION / RULES on **Go —** + **Files:**; STATUS / MAP on **push to git all good** only
- **Literal ask** — option ≠ default; one line = one change unless user lists more
- **One playtest bug** — failing test + related fixes
- **WHEN** — lock in DECISIONS before UI; Jest must match
- **Prototype ≠ production** — no shared code with `src/`
- **Static assets** — from `public/`; no large base64 in TS bundles
- **Never edit** `VISION/CLAUDE_TEST_REPORT_05.md`
- **Cleanup on Go only** — no auto doc sync
